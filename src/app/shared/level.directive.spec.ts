import { LevelDirective } from './level.directive';

describe('LevelDirective', () => {
  it('should create an instance', () => {
    const directive = new LevelDirective();
    expect(directive).toBeTruthy();
  });
});
